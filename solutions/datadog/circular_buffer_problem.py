"""
Implement a fixed-size circular buffer (FIFO queue) with:
- enqueue(element): Add element, raise error if full
- dequeue(): Remove and return oldest element, raise error if empty
- peek(): View oldest element without removing
- is_full(), is_empty(), size()
"""